// handles the data
public class Tiles {
	private String[][] board = new String[9][9];
	private int[] placed = new int[9];
	private int lastCol;

	public Tiles() {
		for (int i = 0; i < 9; i++) {
			placed[i] = 0;
			for (int j = 0; j < 9; j++) {
				board[i][j] = " ";
			}
		}
	}

	// changes the board data, returns **true** if the selected column **is full**.
	public boolean placeTile(Player p, int column) {
		column -= 1;
		if (placed[column] > 8) {
			return true;
		} else {
			board[placed[column]][column] = p.symbol();
			placed[column]++;
			lastCol = column;
			return false;
		}
	}

	public boolean gameOverCheck(Player p) {
		// check vertical
		int vcount = 0;
		
		for (int i = placed[lastCol]; i >= 0 ; i--) {
			if (board[i][lastCol].equals(p.symbol())) {
				vcount++;
				if (vcount==4) {
					p.win();
					return true;
				}
			}
		}
		return false;
	}

	public String[][] Board() {
		return this.board;
	}
}
